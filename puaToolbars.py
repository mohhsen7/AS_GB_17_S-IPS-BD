import re


class puaToolbars:
    def checkPuaToolbarsTCP(payloadToBeChecked):
        #patternT1 = re.compile("\/update\/barcab\/|tn=|baiducb|id=|version=",flags=2)
        #patternT2 = re.compile("\.gif\?action=|&browser=|&osbuild=|&osprod=",flags=2)   
        #patternT3 = re.compile("\.gif\?report=|&f=|&n=|&rnd=",flags=2) 
        
        #if re.search(patternT1, payloadToBeChecked):
            #print("Alert!!!\t","PUA-TOOLBARS Hijacker baidu toolbar runtime detection - updates automatically")
        #if re.search(patternT2, payloadToBeChecked):
            #print("Alert!!!\t","PUA-TOOLBARS Win.Toolbar.Crossrider variant outbound connection") 
        #if re.search(patternT3, payloadToBeChecked):
            #print("Alert!!!\t","PUA-TOOLBARS Win.Toolbar.Crossrider variant outbound connection")  
        return